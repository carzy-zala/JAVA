// package basics;

public class operators {

    public static void main(String[] arg)
    {
        //all operator are same as the c++
        //+,-,*,/,% all are same 
        //z++ and ++z concept same as c++
        //x+=2 := x=x+2 same as c++ (augmented variable)
        //have same priority like c++
        //use () to change priority
        //we use explicite type convertion as below

        byte aa= 9 ;int b=25;
        float res = (float)aa + b;  //we use (double) to convert it in double and many more
        System.out.println(res);

        int a=5;
        System.out.println("thank you !! \n"+a);
    }
    
}
